import React from 'react';

const Header = (props) => {
    return (
    <header>
        <div className="header" id="myHeader">
            <h2>Simon</h2>
        </div>
    </header>
    );
}

export default Header;